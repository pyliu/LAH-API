if (Vue) {
  Vue.component('lah-court-log-search', {
    template: `<b-card>
      製作中 ... 
    </b-card>`,
    props: {
      sample: {
        type: Boolean,
        default: false
      }
    },
    data: () => ({ }),
    methods: {},
    watch: { },
    computed: { },
    created() { },
    mounted() { }
  });
}